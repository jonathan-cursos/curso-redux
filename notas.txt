Store

Dependencias:
	-redux.
	-react-redux.

Creación del Store:
	-En el index de nuestra app, importaremos:
		.createStore de redux.
		.Provider de react-redux.
	-Creamos una const, cuyo valor sea createStore(), tendrá 2 params:
		.Todos los reducers (por ahora, un obj. vacío).
		.El estado inicial.

-Iremos avanzando para ver que hacer con este store.