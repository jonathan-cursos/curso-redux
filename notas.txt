Evitar sobrescritura

-En esta clase presentamos el error y carga, y tambien solucionaremos el problema de la clase anterior.
-Para solucionarlo, en el componente, llamaremos al action de traer publicaciones, pero solo cuando no esté el key
	'publicaciones_key' dentro del reducer de usuarios.
-Para saber que un objeto tenga un key, para validar sí un objeto tiene un key se usa 'in':
	.'key_ejemplo' in objetoEjemplo: Devuelve true sí el key_ejemplo está dentro de objetoEjemplo.
-Sí necesitamos pasar eso a negativo: !('key_ejemplo' in objetoEjemplo)