Agregando funciones a la app con React

-En una función podemos retornar un array, que cada elemento sea JSX y renderizarlos.
-Para renderizarlos, llamamos dentro del return a la función, pero debemos hacerlo con ().
-Por ejemplo: {array()}